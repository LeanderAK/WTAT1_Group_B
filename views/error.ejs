<body>
<div>
    <% if (error) { %>
        <h1><%= error.toString().charAt(0).toUpperCase() + error.slice(1) %> not found</h1>
        <p>Sorry, but the requested <%= error %> cannot be found</p>
    <% } else { %>
        <h1>404 - Page Not Found</h1>
        <p>Sorry, but the requested URL does not lead anywhere</p>
    <% } %>
</div>
</body>